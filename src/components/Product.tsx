import React, {useRef} from 'react';

type ProductProps = {
};

const Product: React.FC<ProductProps> = () => {
    return (
        <div>
            Product
        </div>
    );
};

export default Product;