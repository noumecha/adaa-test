import React, {useRef} from 'react';

type ButtonProps = {
};

const Button: React.FC<ButtonProps> = () => {
    return (
        <div>
            button
        </div>
    );
};

export default Button;